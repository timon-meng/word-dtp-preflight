---
name: word-dtp-preflight
description: "DTP Word文件预检与质检工具（Word DTP Preflight）。当用户上传或指定一个 .docx 文件并要求进行预检（preflight）、质检（QC）、DTP检查、文件检查时触发。能检测：缺失字体/图片、交叉引用错误、多余换行、页码问题、隐藏文字、编号列表不一致、数字比对；也能执行写操作：取消隐藏全部文字、伪文字替换（pseudonymization）。适用于 DTP 流水线中的 Preflight 和 DTP 阶段。即使用户只说「帮我检查一下这个 Word 文件」也应触发本技能。"
---

# Word DTP Preflight Skill

## 概述

本技能针对 DTP（桌面排版）生产流水线，对 `.docx` 文件执行一系列自动化质检与修复操作，对应以下需求：

| 编号 | 需求名称 | 类型 |
|------|---------|------|
| R03 | 缺失字体 / 图片检查 | 分析 |
| R04 | 交叉引用检查 | 分析 |
| R05 | 多余换行检查 | 分析 |
| R09 | 页码检查 | 分析 |
| R20 | 隐藏文字检查 | 分析 |
| R21 | 编号 / 列表比对 | 分析（需源文件） |
| R29 | 数字比对检查 | 分析（需源文件） |
| R25 | 取消隐藏全部文字 | 写操作 |
| R16 | 伪文字替换（Pseudonymization） | 写操作 |

---

## 工作流程

### Step 1：确认用户意图

用户会提供一个（或多个）`.docx` 文件，并说明他们想做什么。常见情况：

- "帮我做预检" → 运行所有分析类检查（R03–R20）
- "检查交叉引用" → 只运行 R04
- "数字比对" → 运行 R29，需要源文件和译文文件各一个
- "取消隐藏" → 执行 R25 写操作
- "伪文字替换" → 执行 R16 写操作

如果用户说「全面检查」或「预检」，默认运行所有**分析类**检查（不执行写操作，除非用户明确要求）。

### Step 2：定位文件

文件可能在：
- 用户上传的 `/sessions/hopeful-beautiful-meitner/mnt/uploads/` 目录下
- 用户选择的工作文件夹 `/sessions/hopeful-beautiful-meitner/mnt/` 下

用 `find` 命令定位 `.docx` 文件后再继续。

### Step 3：安装依赖

```bash
pip install python-docx lxml --break-system-packages -q
```

### Step 4：运行检查脚本

每项检查都有对应脚本，位于本技能的 `scripts/` 目录。

**运行方式（分析类）：**
```bash
python3 <skill_dir>/scripts/<check_name>.py <file.docx> [source_file.docx]
```

**运行方式（写操作类）：**
```bash
python3 <skill_dir>/scripts/<operation>.py <file.docx> <output_file.docx>
```

所有脚本输出 JSON，字段见下方各脚本说明。

### Step 5：汇总报告

用中英文双语格式输出一份结构化报告，格式见「报告格式」部分。将结果文件（如有写操作输出）保存到工作文件夹并提供链接。

---

## 各检查脚本说明

详见 `references/checks.md`，内含每个检查的逻辑说明和输出字段。

脚本清单：

| 脚本文件 | 对应需求 | 说明 |
|---------|---------|------|
| `check_fonts_images.py` | R03 | 检查缺失字体和图片 |
| `check_cross_references.py` | R04 | 检查失效交叉引用、TOC、书签 |
| `check_returns.py` | R05 | 检查多余硬回车和软回车 |
| `check_page_numbers.py` | R09 | 检查页码连续性和格式 |
| `check_hidden_text.py` | R20 | 检查隐藏文字和溢出文本框 |
| `check_numbering.py` | R21 | 比对源文件与译文的编号/列表 |
| `check_numbers.py` | R29 | 比对源文件与译文的数字 |
| `unhide_text.py` | R25 | 取消隐藏全部文字（写操作） |
| `pseudonymize.py` | R16 | 伪文字替换（写操作） |

---

## 报告格式

每次检查完成后，输出如下格式（Markdown）：

```
# Word DTP 预检报告
**文件**：[filename]
**检查时间**：[timestamp]
**执行的检查**：R03, R04, R05 ...

---

## ✅ / ⚠️ / ❌  [检查名称]
**状态**：通过 / 警告 / 错误  
**发现**：X 处问题

| 位置 | 问题描述 |
|------|---------|
| 第3段 | 多余硬回车 |

---

## 总结
| 检查项 | 状态 | 问题数 |
|--------|------|--------|
| 缺失字体/图片 | ✅ 通过 | 0 |
| 交叉引用 | ❌ 错误 | 3 |
...
```

对于写操作（R25、R16），额外提供处理后文件的下载链接。

---

## 重要注意事项

- **永远不要修改用户的原始文件**。写操作必须输出到 `_cleaned` / `_unhidden` / `_pseudo` 后缀的新文件。
- 如果文件很大（>5MB），部分检查可能较慢，提前告知用户。
- R21 和 R29 需要**源文件**和**译文文件**各一个，如果用户只提供了一个文件，询问另一个文件。
- 检查完成后，如果发现多个问题，用中文简明解释每个问题的含义，不要只堆叠技术术语。
